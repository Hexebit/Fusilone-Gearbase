<UserControl x:Class="Fusilone.Views.DeviceKanbanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Fusilone.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style x:Key="KanbanColumnCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource FslSurface}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource FslBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style x:Key="KanbanItemCard" TargetType="materialDesign:Card">
            <Setter Property="Background" Value="{DynamicResource FslSurface2}"/>
            <Setter Property="UniformCornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10,10,10,0"/>
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource FslBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SÜTUN 1: BEKLEYEN (PASİF) -->
        <Border Grid.Column="0" Style="{StaticResource KanbanColumnCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Margin="15,15,15,10">
                    <TextBlock Text="{DynamicResource Kanban_Pending}" Foreground="{DynamicResource FslTextSecondary}" FontWeight="Black" FontSize="11"/>
                    <Border Height="2" Background="{DynamicResource StatusColor_Pending}" Width="40" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListPending">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Style="{StaticResource KanbanItemCard}" Cursor="Hand" MouseLeftButtonUp="Card_MouseLeftButtonUp">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Id}" FontWeight="Bold" Foreground="{DynamicResource StatusColor_Pending}" FontSize="13"/>
                                            <TextBlock Text="{Binding FullName}" FontSize="12" Foreground="{DynamicResource FslTextPrimary}" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding OwnerName}" FontSize="11" Foreground="{DynamicResource FslTextSecondary}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Border Grid.Column="1" Width="36" Height="36" CornerRadius="10" ClipToBounds="True" VerticalAlignment="Top">
                                            <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                        </Border>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- SÜTUN 2: İŞLEMDE (ARIZALI) -->
        <Border Grid.Column="1" Style="{StaticResource KanbanColumnCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Margin="15,15,15,10">
                    <TextBlock Text="{DynamicResource Kanban_InProgress}" Foreground="{DynamicResource FslTextSecondary}" FontWeight="Black" FontSize="11"/>
                    <Border Height="2" Background="{DynamicResource StatusColor_InProgress}" Width="40" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListInProgress">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Style="{StaticResource KanbanItemCard}" Cursor="Hand" MouseLeftButtonUp="Card_MouseLeftButtonUp">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Id}" FontWeight="Bold" Foreground="{DynamicResource StatusColor_InProgress}" FontSize="13"/>
                                            <TextBlock Text="{Binding FullName}" FontSize="12" Foreground="{DynamicResource FslTextPrimary}" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding OwnerName}" FontSize="11" Foreground="{DynamicResource FslTextSecondary}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Border Grid.Column="1" Width="36" Height="36" CornerRadius="10" ClipToBounds="True" VerticalAlignment="Top">
                                            <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                        </Border>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- SÜTUN 3: HAZIR (AKTİF) -->
        <Border Grid.Column="2" Style="{StaticResource KanbanColumnCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Margin="15,15,15,10">
                    <TextBlock Text="{DynamicResource Kanban_Ready}" Foreground="{DynamicResource FslTextSecondary}" FontWeight="Black" FontSize="11"/>
                    <Border Height="2" Background="{DynamicResource StatusColor_Ready}" Width="40" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ListReady">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Style="{StaticResource KanbanItemCard}" Cursor="Hand" MouseLeftButtonUp="Card_MouseLeftButtonUp">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Id}" FontWeight="Bold" Foreground="{DynamicResource StatusColor_Ready}" FontSize="13"/>
                                            <TextBlock Text="{Binding FullName}" FontSize="12" Foreground="{DynamicResource FslTextPrimary}" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding OwnerName}" FontSize="11" Foreground="{DynamicResource FslTextSecondary}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Border Grid.Column="1" Width="36" Height="36" CornerRadius="10" ClipToBounds="True" VerticalAlignment="Top">
                                            <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                        </Border>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
